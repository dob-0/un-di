# Notes

Write anything here — ideas, complaints, things that felt out of sync.
No format required. We'll go through it together.

---

## Open items



---

## Resolved

- **Is un-di connected to di.iiii / future projects?** — Resolved 2026-06-19: only global `~/.claude/` config (settings.json, CLAUDE.md) is live/automatic, via the Stop hook backup. Project-level config (di.iiii's `.claude/`, `AGENTS.md`, `.mcp.json`) stays independent per project, manually bootstrapped from un-di's `templates/` — no live sync, by choice (project configs genuinely diverge). See `docs/claude/new-project.md` and `DONE.md`.